import Layout from '@/components/ui/Layout/Layout';
import React, { FC } from 'react';

const MovieEdit: FC = () => {
	return <Layout title="Movie edit">MovieEdit</Layout>;
};

export default MovieEdit;
